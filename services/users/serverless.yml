service: mono-repo-sls-users

provider:
  name: aws
  runtime: nodejs8.10

functions:
  hello:
    handler: handler1.hello
    events:
      - http:
          path: /
          method: get
          cors: true
