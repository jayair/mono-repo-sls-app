service: mono-repo-sls-users

provider:
  name: aws
  runtime: nodejs9.10

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: get
          cors: true
